<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Simple Twitter", tweets_path, class: "navbar-brand" %>
    </div>

   
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li><%= link_to 'Admin Panel', admin_tweets_path, class: active_class(tweets_path) if current_user.role == "admin" %></li>
          <li><%= link_to 'Front End', tweets_path, class: active_class(admin_tweets_path) if current_user.role == "admin" %></li>
          <li><%= link_to current_user.name, tweets_user_path(current_user) %></li>
          <li><%= link_to 'Log Out', destroy_user_session_path, method: :delete, class: "btn btn-default" %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>